name: .NET Build and Test with Coverage

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup .NET SDK
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'   # or 8.0.x based on your project target

      # 3️⃣ Restore dependencies
      - name: Restore dependencies
        run: dotnet restore CalculatorApp.sln

      # 4️⃣ Build the solution
      - name: Build solution
        run: dotnet build CalculatorApp.sln --no-restore --configuration Release

      # 5️⃣ Run unit tests with coverage (MSBuild integration)
      - name: Run tests with coverage
        run: |
          mkdir -p TestResults/coverage

          echo "🧪 Searching for test projects..."
          find . -name "*.Tests.csproj"

          for proj in $(find . -name "*.Tests.csproj"); do
            echo "➡️ Running tests for $proj"
            dotnet test "$proj" \
              --no-build \
              --configuration Release \
              /p:CollectCoverage=true \
              /p:CoverletOutput=../../TestResults/coverage/ \
              /p:CoverletOutputFormat=cobertura
          done

          echo "🔍 Checking for coverage file..."
          find ./TestResults -name "coverage.cobertura.xml" || true

          if [ ! -f ./TestResults/coverage/coverage.cobertura.xml ]; then
            echo "❌ coverage.cobertura.xml NOT FOUND!"
            exit 1
          else
            echo "✅ coverage.cobertura.xml FOUND!"
          fi

      # 6️⃣ Generate coverage report (HTML + Cobertura)
      - name: Generate coverage report
        uses: danielpalme/ReportGenerator-GitHub-Action@5.3.8
        with:
          reports: './TestResults/coverage/coverage.cobertura.xml'
          targetdir: './CodeCoverageReport'
          reporttypes: 'HtmlSummary;Cobertura'

      #
